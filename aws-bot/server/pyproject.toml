[project]
name = "aws-bot-server"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "aws-xray-sdk>=2.15.0",
    "boto3>=1.43.82",
    "fastapi>=0.141.1",
    "mangum>=0.22.0",
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "uvicorn[standard]>=0.52.4",
]
