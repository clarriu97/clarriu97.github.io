# Force pnpm usage
engine-strict=true

# Hoist pattern for better compatibility
shamefully-hoist=false
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*

# Faster installations
prefer-frozen-lockfile=true
